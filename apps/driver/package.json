{
  "name": "@yatra-seva/driver",
  "version": "0.1.0",
  "private": true,
  "description": "YatraSeva Driver/Captain mobile application",
  "main": "expo-router/entry",
  "scripts": {
    "dev": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "build": "echo \"Mobile apps are built via EAS\" && exit 0",
    "type-check": "tsc --noEmit",
    "lint": "echo \"lint placeholder\" && exit 0",
    "test": "echo \"No tests yet\" && exit 0",
    "clean": "rm -rf .expo dist"
  },
  "dependencies": {
    "@yatra-seva/shared-types": "*",
    "expo": "~53.0.0",
    "expo-location": "~18.0.7",
    "expo-router": "~5.0.0",
    "expo-status-bar": "~2.2.0",
    "react": "^19.0.0",
    "react-native": "~0.79.0",
    "react-native-safe-area-context": "~5.4.0",
    "react-native-screens": "~4.10.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.8.0"
  }
}
