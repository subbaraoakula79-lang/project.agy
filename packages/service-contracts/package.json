{
  "name": "@yatra-seva/service-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Service interface contracts for mock/real provider switching",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@yatra-seva/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
